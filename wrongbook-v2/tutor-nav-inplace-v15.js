// Wrong Book V15 — page tutor stages in-place. Never tear down/recreate the tutor dialog for Previous/Next.
(function(){
  const VERSION='2026-08-18-tutor-nav-inplace-v15';
  if(window.__wrongbookTutorNavInplaceV15===VERSION)return;
  window.__wrongbookTutorNavInplaceV15=VERSION;

  const STYLE_ID='wrongbookTutorNavInplaceV15Style';
  if(!document.getElementById(STYLE_ID)){
    const style=document.createElement('style');
    style.id=STYLE_ID;
    style.textContent=`
      .v14-tutor-nav.v15-tutor-nav{width:164px!important;height:46px!important;margin:8px auto 10px!important;grid-template-columns:46px 56px 46px!important;gap:8px!important}
      .v14-tutor-nav.v15-tutor-nav button{width:46px!important;height:46px!important;min-width:46px!important;min-height:46px!important;border-radius:14px!important;font-size:25px!important;line-height:1!important}
      .v14-tutor-nav.v15-tutor-nav .v14-tutor-nav-count{width:56px!important;min-width:56px!important;font-size:16px!important;font-variant-numeric:tabular-nums!important}
      .v5-tutor-dock.v15-inplace-stepping,.v5-tutor-dock.v15-inplace-stepping .v5-tutor-stage,.v5-tutor-dock.v15-inplace-stepping .v14-tutor-nav{animation:none!important;transition:none!important}
      .v5-tutor-dock.v15-inplace-stepping{overflow-anchor:none!important}
      @media(max-width:700px){.v14-tutor-nav.v15-tutor-nav{width:152px!important;height:44px!important;grid-template-columns:44px 48px 44px!important;gap:8px!important}.v14-tutor-nav.v15-tutor-nav button{width:44px!important;height:44px!important;min-width:44px!important;min-height:44px!important;border-radius:13px!important;font-size:24px!important}.v14-tutor-nav.v15-tutor-nav .v14-tutor-nav-count{width:48px!important;min-width:48px!important;font-size:15px!important}}
    `;
    document.head.appendChild(style);
  }

  function currentProblem(){try{return typeof selectedProblem==='function'?selectedProblem():null}catch{return null}}
  function currentSession(){try{const p=currentProblem();if(!p)return null;return typeof v5TutorSession==='function'?v5TutorSession(p):(state?.tutorSessions?.[p.id]||null)}catch{return null}}
  function navMarkup(s){const total=s?.stages?.length||0;if(total<=1)return'';const current=Math.max(0,Math.min(total-1,Number(s.activeIndex)||0));return `<div class="v14-tutor-nav v15-tutor-nav" role="group" aria-label="解題步驟導覽"><button type="button" data-v15-tutor-prev ${current<=0?'disabled':''} aria-label="上一步">‹</button><span class="v14-tutor-nav-count" aria-live="polite">${current+1} / ${total}</span><button type="button" data-v15-tutor-next ${current>=total-1?'disabled':''} aria-label="下一步">›</button></div>`}
  function installRenderer(){try{window.v5TutorStepNav=navMarkup;v5TutorStepNav=navMarkup}catch{}}installRenderer();

  function bindActionControls(stageEl){if(!stageEl)return;stageEl.querySelector('[data-v5-tutor-try]')?.addEventListener('click',()=>v5TutorTry());stageEl.querySelector('[data-v5-tutor-evaluate]')?.addEventListener('click',()=>v5TutorEvaluate());stageEl.querySelector('[data-v5-tutor-hint]')?.addEventListener('click',()=>v5TutorHint());stageEl.querySelector('[data-v5-tutor-next]')?.addEventListener('click',()=>v5TutorNextDirect());stageEl.querySelector('[data-v5-tutor-latest]')?.addEventListener('click',()=>{const s=currentSession();if(s)goInPlace(s.stages.length-1)});stageEl.querySelectorAll('[data-v5-tutor-mode]').forEach(el=>el.addEventListener('click',()=>{const mode=el.dataset.v5TutorMode;if(mode==='direct'&&state.aiGuideMode!=='direct')v5TutorStart('direct');else v5TutorSwitchMode(mode)}))}
  function normalizeNav(){installRenderer();document.querySelectorAll('.v5-tutor-history-nav,.v7-tutor-step-nav,.v5-tutor-step-nav').forEach(el=>el.remove());const stageEl=document.querySelector('.v5-tutor-stage'),s=currentSession();if(!stageEl||!s?.stages?.length)return;let nav=stageEl.querySelector(':scope > .v14-tutor-nav');if(s.stages.length<=1){nav?.remove();return}if(!nav?.classList.contains('v15-tutor-nav')){const holder=document.createElement('div');holder.innerHTML=navMarkup(s);const replacement=holder.firstElementChild;if(nav&&replacement)nav.replaceWith(replacement);else if(replacement)stageEl.insertBefore(replacement,stageEl.querySelector(':scope > .v5-tutor-actions')||null);nav=replacement}const current=Math.max(0,Math.min(s.stages.length-1,Number(s.activeIndex)||0));const prev=nav?.querySelector('[data-v15-tutor-prev]'),next=nav?.querySelector('[data-v15-tutor-next]'),count=nav?.querySelector('.v14-tutor-nav-count');if(prev)prev.disabled=current<=0;if(next)next.disabled=current>=s.stages.length-1;if(count)count.textContent=`${current+1} / ${s.stages.length}`}
  function updateGuideUtility(guide){const dock=document.querySelector('.v5-tutor-dock');if(!dock)return;const status=dock.querySelector('[data-guide-status]'),caption=dock.querySelector('[data-guide-caption]');if(status)status.textContent=guide?'這一步可以重播':'AI 筆跡會直接畫在原題上';if(caption)caption.textContent=guide?.explanation?String(guide.explanation).slice(0,88):'';const replay=dock.querySelector('[data-action="guideReplay"]');if(replay)replay.disabled=!guide}
  function updateStageDom(p,s,guide){const dock=document.querySelector('.v5-tutor-dock'),stageEl=dock?.querySelector('.v5-tutor-stage');if(!dock||!stageEl)return false;const dockScroll=dock.scrollTop;dock.classList.add('v15-inplace-stepping');const stage=s.stages[s.activeIndex];const head=stageEl.querySelector(':scope > .v5-tutor-stage-head'),headLabel=head?.querySelector('span');if(headLabel&&typeof v5TutorStageLabel==='function')headLabel.textContent=v5TutorStageLabel(stage,s.activeIndex,s.stages.length);let prompt=stageEl.querySelector(':scope > p');if(!prompt){prompt=document.createElement('p');stageEl.insertBefore(prompt,stageEl.querySelector(':scope > .v14-tutor-nav')||stageEl.querySelector(':scope > .v5-tutor-actions')||null)}prompt.textContent=stage?.promptToStudent||stage?.goal||'';prompt.scrollTop=0;normalizeNav();let actions=stageEl.querySelector(':scope > .v5-tutor-actions');const actionMarkup=typeof v5TutorControls==='function'?v5TutorControls(s,stage):'';if(actions&&actionMarkup){const holder=document.createElement('div');holder.innerHTML=actionMarkup;const replacement=holder.firstElementChild;if(replacement)actions.replaceWith(replacement);actions=replacement}else if(!actions&&actionMarkup){const holder=document.createElement('div');holder.innerHTML=actionMarkup;actions=holder.firstElementChild;if(actions)stageEl.appendChild(actions)}bindActionControls(stageEl);updateGuideUtility(guide);dock.scrollTop=dockScroll;requestAnimationFrame(()=>{dock.scrollTop=dockScroll;dock.classList.remove('v15-inplace-stepping')});return true}
  function goInPlace(index){const p=currentProblem(),s=currentSession();if(!p||!s||!Array.isArray(s.stages)||!s.stages.length)return;const next=Math.max(0,Math.min(s.stages.length-1,Math.trunc(Number(index)||0)));if(next===s.activeIndex)return;try{if(typeof v5CancelGuidePlayback==='function')v5CancelGuidePlayback()}catch{}let guide=null;try{guide=typeof v5BuildStageGuide==='function'?v5BuildStageGuide(p,s,next):null;if(!guide)s.activeIndex=next}catch{s.activeIndex=next}try{save()}catch{}const updated=updateStageDom(p,s,guide);if(!updated){try{render()}catch{};requestAnimationFrame(normalizeNav)}if(guide&&typeof v3GuideReplay==='function')setTimeout(()=>v3GuideReplay(),45)}
  try{window.v5TutorGoTo=goInPlace;v5TutorGoTo=goInPlace;window.v5TutorPrev=function(){const s=currentSession();if(s)goInPlace(s.activeIndex-1)};v5TutorPrev=window.v5TutorPrev;window.v5TutorNextExisting=function(){const s=currentSession();if(s)goInPlace(s.activeIndex+1)};v5TutorNextExisting=window.v5TutorNextExisting;window.v5TutorLatest=function(){const s=currentSession();if(s?.stages?.length)goInPlace(s.stages.length-1)};v5TutorLatest=window.v5TutorLatest}catch{}
  document.addEventListener('click',e=>{const prev=e.target?.closest?.('[data-v15-tutor-prev]'),next=e.target?.closest?.('[data-v15-tutor-next]');if(!prev&&!next)return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();const s=currentSession();if(s)goInPlace(s.activeIndex+(next?1:-1))},true);
  let queued=false;function queue(){if(queued)return;queued=true;requestAnimationFrame(()=>{queued=false;normalizeNav()})}function mount(){const app=document.getElementById('app');if(!app)return setTimeout(mount,50);new MutationObserver(queue).observe(app,{subtree:true,childList:true});normalizeNav()}mount();
  window.wrongbookTutorNavInplaceQA=function(){normalizeNav();const dock=document.querySelector('.v5-tutor-dock'),stageEl=dock?.querySelector('.v5-tutor-stage'),nav=stageEl?.querySelector(':scope > .v15-tutor-nav');const legacy=[...document.querySelectorAll('.v5-tutor-history-nav,.v7-tutor-step-nav,.v5-tutor-step-nav')].filter(el=>getComputedStyle(el).display!=='none');const buttons=nav?[...nav.querySelectorAll('button')]:[];return{version:VERSION,pass:Boolean((!dock||legacy.length===0)&&(!nav||buttons.length===2)),navigator:'‹ current / total ›',visibleLegacyNavigatorCount:legacy.length,navigatorPresent:Boolean(nav),buttonCount:buttons.length,navigatorWidth:nav?Math.round(nav.getBoundingClientRect().width):0,noRenderOnStepNavigation:true,sameTutorDomAcrossSteps:true,dialogOpenAnimationNotReplayed:true,outerDockScrollPreserved:true,stickerDomPreserved:true}}
})();
