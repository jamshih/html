name: Hearframe Live Audio Refine QA

on:
  push:
    branches: [main]
    paths:
      - 'hearframe-grand-v4/live-audio-refine-qa-trigger.txt'

permissions:
  contents: write

jobs:
  qa:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install WebKit
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps webkit
      - name: Run live iPad/WebKit audio refinement
        id: qa
        continue-on-error: true
        run: node .github/scripts/hearframe-live-audio-refine-qa.mjs
      - name: Record result
        if: always()
        run: |
          git config user.name 'hearframe-live-audio-refine-qa'
          git config user.email 'actions@users.noreply.github.com'
          git add hearframe-grand-v4/live-audio-refine-qa.json || true
          git commit -m 'Record live Hearframe speech-audio QA [skip ci]' || true
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Fail if live speech QA failed
        if: steps.qa.outcome != 'success'
        run: exit 1
